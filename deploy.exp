#!/usr/bin/expect -f
set timeout 60
spawn ssh -o StrictHostKeyChecking=no root@147.93.155.122 "cd /opt/Fatheherp-website && git pull origin master && docker compose build && docker compose up -d"
expect "password:"
send "enfonotech\r"
expect eof
